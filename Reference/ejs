npm i ejs

app.set("view engine", "ejs");

need a views folder.
and all html docs asved as ejs

connection.query(q, function (error, results) {
res.render("home", {data: count});                                  // This bit passes value to \views\html.ejs
 });
 
 
 <%=data%>                                                          // add this to the html.ejs page to display the variable
